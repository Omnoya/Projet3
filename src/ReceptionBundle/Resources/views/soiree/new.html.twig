{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="titre">Créer un Plat</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <form>
            <a href="{{ path('soiree_index') }}">
                <input id="button" type="button" value="Back to the list";">
            </a>
        </form>
    </ul>
{% endblock %}
